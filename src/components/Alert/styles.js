const styles = theme => ({
  container: {
    margin: 0,
  },
  closeButton: {
    position: 'absolute',
    right: theme.spacing(1),
    top: theme.spacing(1),
    color: theme.palette.ACCENT_BLUE,
  },
  content: {
    padding: '0 16px 32px 24px',
  },
});

export default styles;
